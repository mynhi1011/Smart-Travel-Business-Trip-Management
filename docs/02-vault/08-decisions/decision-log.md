# Decision Log - Smart Travel & Business Trip Management

**Dự án:** Smart Travel & Business Trip Management
**Nhóm:** Nhóm 11 - MIS3032_1
**Mục đích:** Ghi lại mọi quyết định quan trọng đã chốt cho MVP, để `source-priority.md` có nguồn tra cứu và để requirements/business-rules/story-specs không phải lặp lại lý do mỗi lần tham chiếu (`D-XX`).

> Quy ước: mỗi entry gồm ID, ngày chốt, nội dung quyết định, lý do, và các artifact bị ảnh hưởng. Không xoá entry cũ khi có thay đổi — đánh dấu `Superseded by D-XX` thay vào đó.

---

| ID | Ngày chốt | Quyết định | Lý do / Nguồn gốc | Artifact bị ảnh hưởng |
|---|---|---|---|---|
| **D-05** | 2026-08-28 | Các dịch vụ tích hợp thật (đặt phòng, thanh toán, ERP, email/SMS) nằm **ngoài phạm vi MVP**: không tích hợp Agoda/Sabre, VNPay/Stripe, SAP/Oracle ERP, hay email/SMS thật — chỉ dùng in-app notification. | Giới hạn thời gian/nguồn lực của đồ án; tập trung vào luồng nghiệp vụ core (Request → Approve → Itinerary → Expense → Close). | `scope.md` (Out of Scope), `architecture.md` §5.6 |
| **D-06** | 2026-08-28 | Chuẩn hoá trần chi phí lưu trú (Accommodation Limit) cho hệ thống mẫu: Staff/Specialist 1.000.000 VNĐ/đêm, Manager/Lead 1.800.000 VNĐ/đêm, Director/Executive 3.000.000 VNĐ/đêm — khác với con số 1.200.000 VNĐ nêu trong phỏng vấn gốc (P3). | Interview notes gốc chỉ là dữ liệu thô, cần một bộ hạn mức rõ ràng theo cấp bậc để build policy engine; nhóm chốt lại thành 3 mức theo job grade thay vì 1 mức chung. | `business-rules.md` (BR-TR-01), `interview-notes.md` (ghi chú tham chiếu ngược lại D-06) |
| **D-07** | 2026-08-28 | SLA nội bộ cho Advance Notice Rule: yêu cầu công tác thông thường phải gửi trước ≥ 3 ngày làm việc (khác với 7 ngày nêu trong phỏng vấn gốc); dưới 3 ngày phải đánh dấu `is_urgent` kèm lý do khẩn cấp. | Rút ngắn từ 7 xuống 3 ngày làm việc cho phù hợp với quy mô hệ thống mẫu và để có đủ ca kiểm thử "khẩn cấp" trong MVP. | `business-rules.md` (BR-TR-03), `US-01-create-trip-request.md` |
| **D-09** | 2026-09-10 | Chu kỳ trạng thái (status) của Trip Request là canonical duy nhất: `DRAFT → SUBMITTED → (APPROVED \| PENDING_ADMIN_APPROVAL → APPROVED \| REJECTED) → ONGOING → EXPENSE_DRAFT → EXPENSE_SUBMITTED → EXPENSE_APPROVED → CLOSED`. `APPROVED_MANAGER` là tên trạng thái **legacy, không còn hợp lệ** — loại bỏ khỏi API/UI/requirements. `MANAGER_REVIEWING` và `MANAGER_REAPPROVE` chỉ là trạng thái **nội bộ/transient** trong cùng transaction, không được expose như trạng thái cuối trong response hay dùng làm filter ở client. | FIX-02: 4 phiên bản trạng thái approval (`APPROVED_MANAGER`, `MANAGER_REVIEWING`, `APPROVED`, `PENDING_ADMIN_APPROVAL`) mâu thuẫn nhau giữa requirements/story-specs/code — cần một chu kỳ duy nhất làm nguồn sự thật trước khi Engineering đồng bộ lại code/OpenAPI. | `requirements.md` (REQ-TR-04/05), `user-stories.md` (US-05, US-06), `business-rules.md` (BR-TR-04), `workflows.md`, `US-05-manager-approve.md` |
| **D-10** | 2026-09-10 | Vượt hạn mức Per Diem (BR-TR-02) **không bao giờ chặn** tạo/submit Trip Request — chỉ hiển thị cảnh báo (`POLICY_VIOLATION_PER_DIEM_EXCEEDED`, severity WARNING) và tự động định tuyến sang phê duyệt Cấp 2 theo BR-TR-04. Việc "chặn" (block) chỉ có thể xảy ra gián tiếp qua yêu cầu duyệt Cấp 2, không phải một lỗi 400 riêng cho Per Diem. | FIX-03: business-rules.md nói "không cho phép" trong khi US-01 và validator nói warning-only, còn code policy.service.ts từng tạo violation severity BLOCKER — mâu thuẫn hành vi cần canonical hoá một chiều duy nhất (warning, không block) để Engineering sửa lại code cho khớp. | `business-rules.md` (BR-TR-02), `US-01-create-trip-request.md`, `US-04-policy-check.md` |
| **D-11** | 2026-09-10 | Owner (Employee tạo Trip) được phép chỉnh sửa Itinerary ở **mọi trạng thái Trip trừ `CLOSED`**. Khi Trip đã `CLOSED`, mọi thao tác ghi lên itinerary bị chặn (`409 TRIP_IMMUTABLE`) theo BR-TR-06. | DOC-02: Story Spec cũ ghi "Tuỳ thiết kế: vẫn cho chỉnh sửa…" cho trạng thái khác Draft — không testable, cần một quy tắc rõ ràng để viết test case. | `US-03-itinerary-builder.md` (E-05), `requirements.md` (REQ-TR-06 — đã cập nhật lại để khớp D-11, tránh mâu thuẫn thứ hạng nguồn theo `source-priority.md`) |
| **D-12** | 2026-09-10 | Export PDF tóm tắt chuyến công tác chỉ dành cho **Employee owner hoặc FINANCE** (không bao gồm `ADMIN`/`TRAVEL_ADMIN`), và chỉ khi Trip đang ở một trong các trạng thái: `APPROVED`, `ONGOING`, `EXPENSE_SUBMITTED`, `EXPENSE_APPROVED`, `CLOSED`. | DOC-05 + DOC-06: role được export PDF và danh sách trạng thái cho phép chưa nhất quán giữa User Story, API docs và code — chốt một quy tắc canonical duy nhất cho role + state. | `US-10-notification-pdf.md` |
| **D-13** | 2026-09-10 | **Q-TR-01 (đóng):** MVP **không** cho phép 1 Trip Request bao gồm nhiều nhân viên đi cùng nhau — mỗi Trip Request chỉ áp dụng cho 1 nhân viên chủ trì (`employeeId` duy nhất, do server set). | Giữ data model và luồng approval đơn giản cho MVP; nhóm phỏng vấn không có yêu cầu bắt buộc về group trip. Chuyển từ Open Question sang Decision Record chính thức theo yêu cầu quản trị Vault (DOC-04). | `open-questions.md` (Q-TR-01), `data-model.md`, `US-01-create-trip-request.md` |
| **D-14** | 2026-09-10 | **Q-TR-02 (đóng):** MVP **không** tự động escalation khi Manager không duyệt sau 48h — hệ thống chỉ gửi thông báo nhắc nhở trong ứng dụng (in-app notification), không tự chuyển cấp phê duyệt. | Tự động escalation cần thêm scheduler/cron job và quy tắc định tuyến phức tạp, ngoài phạm vi MVP (xem D-05). Chuyển từ Open Question sang Decision Record chính thức theo yêu cầu quản trị Vault (DOC-04). | `open-questions.md` (Q-TR-02), `US-05-manager-approve.md`, `requirements.md` (REQ-TR-11) |

---

## Ghi chú quản trị (Change Control)

- Mọi entry trong bảng trên là **nguồn ưu tiên thứ 2** sau Business Rules/Requirements đã Confirmed, theo `source-priority.md`.
- Khi một Open Question trong `open-questions.md` được chốt hướng xử lý cho MVP, entry tương ứng phải được nhân bản vào đây dưới dạng Decision Record chính thức (không chỉ ghi chú "Trạng thái: đã quyết định" trong open-questions.md) — đây là fix cho DOC-04.
- File này được `docs/00-project-index.md`, `docs/02-vault/00-index.md` và `docs/02-vault/source-priority.md` tham chiếu trực tiếp; trước đây các link này bị hỏng vì file chưa tồn tại.
