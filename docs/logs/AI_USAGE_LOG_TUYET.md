# AI Usage Log - Nguyễn Thị Ánh Tuyết (Engineering)

**Vai trò trong nhóm:** Engineering (Backend & System Architecture)
**Nhóm:** Nhóm 11 — MIS3032_1
**Môn học:** MIS3032_1

Ghi lại **mọi** lần Tuyết dùng AI có ảnh hưởng tới artifact kỹ thuật của dự án. Mỗi dòng phải có input/context thật, output thật, cách kiểm chứng thủ công, và quyết định cuối cùng (kể cả khi từ chối đề xuất AI).

Format cột theo đúng mẫu giáo trình (mục 2.4 và 19.5): `ID | Ngày | Task/Story | Mục tiêu | Context/Input | AI/Tool & Skill | Output | Human verification | Quyết định cuối cùng | Time/quality impact | Artifact link`.

---

| ID | Ngày | Task/Story | Mục tiêu | Context/Input đưa cho AI | AI/Tool & Skill dùng | Output AI trả về | Human verification | Quyết định cuối cùng | Time/quality impact | Artifact link |
|---|---|---|---|---|---|---|---|---|---|---|
| T-01 | 28/08/2026 | Architecture Design (ADR-01…07) | Thiết kế kiến trúc hệ thống Layered Monolith phù hợp nhóm 5 người, 1 kỹ sư Engineering | requirements.md, business-rules.md (BR-TR-01…07), quy mô nhóm nhỏ, yêu cầu demo cuối kỳ, ràng buộc không dùng cloud-managed service | Kiro (Claude-based, chat) | architecture.md đầy đủ: tech stack (React + Ant Design v5, Node.js + Express + Prisma + PostgreSQL 16, Gemini AI, JWT, SSE), 7 ADR, middleware chain (authGuard → roleGuard → immutableGuard), state machine 13 trạng thái Trip | Tuyết trace REQ-TR-01 → architecture flow → API endpoint → DB table để kiểm tra không có layer "mồ côi"; kiểm tra 7 ADR có đủ lý do và alternatives đã cân nhắc | Chấp nhận kiến trúc tổng thể; yêu cầu AI bổ sung immutableGuard middleware riêng (BR-TR-06) mà bản nháp đầu bỏ sót — middleware này cần chặn write mutation khi `trip.status = CLOSED` trước khi request chạm controller | Viết tay ước tính 8 giờ → có AI hỗ trợ 4 giờ | `docs/05-technical/architecture.md` |
| T-02 | 28/08/2026 | Data Model & ERD (10 bảng) | Thiết kế schema đảm bảo ACID, không race condition, enforce business rules ở DB level | architecture.md (đã duyệt), business-rules.md (BR-TR-01…07), yêu cầu UUID PK, BIGINT tiền VNĐ, TIMESTAMPTZ, snapshot pattern | Kiro (Claude-based, chat) | data-model.md đầy đủ: 10 bảng với DDL SQL, Prisma Schema, 13 enum types, index strategy, constraint CHECK, UNIQUE INDEX chống double-approve, trigger immutable cho audit_logs | Tuyết kiểm tra: (1) mọi FK có ON DELETE hợp lý; (2) `approval_records` có UNIQUE INDEX chống double-approve; (3) `audit_logs` không có `updated_at` (INSERT-only); (4) snapshot fields đúng ngữ nghĩa (không thay đổi khi entity gốc đổi) | Chấp nhận phần lớn; yêu cầu AI sửa lại kiểu dữ liệu `variance_pct` từ `FLOAT` sang `NUMERIC(6,2)` để tránh floating-point error khi so sánh threshold 10% trong BR-TR-05 | Viết tay ước tính 6 giờ → có AI hỗ trợ 3 giờ | `docs/05-technical/data-model.md`, `docs/05-technical/ERD.svg` |
| T-03 | 28/08/2026 | API Contract (35 endpoints) | Định nghĩa API contract đầy đủ: request/response schema, RBAC matrix, error codes, server-computed fields | architecture.md, data-model.md, business-rules.md, user-stories.md (US-01…10) | Kiro (Claude-based, chat) | API.md + openapi.yaml: 35 endpoints, role permission matrix (5 roles × 8 nhóm endpoint), danh sách server-computed fields client không được tự gửi, error response chuẩn, pagination convention | Tuyết kiểm tra: (1) mọi endpoint có `roleGuard` đúng role; (2) `tripDays`, `variancePct`, `isUrgent` nằm trong server-computed list; (3) `POST /trips/:id/approve` khác behavior với Manager (L1) và Travel Admin (L2) | **Từ chối một phần:** AI gộp chung `approve` L1/L2 vào cùng 1 endpoint mà không kiểm tra role cụ thể ở tầng middleware — yêu cầu AI sửa lại: cùng URL nhưng behavior phân biệt theo `req.user.role` + trip status; bổ sung ghi chú security về resource-level authorization (manager chỉ duyệt subordinate của mình) | Viết tay ước tính 6 giờ → có AI hỗ trợ 3 giờ | `docs/05-technical/API.md`, `docs/05-technical/openapi.yaml` |
| T-04 | 28/08/2026 | Story Specs kỹ thuật (US-01…US-10) | Mỗi user story có tech spec: API call, DB transaction, error cases, sequence diagram | user-stories.md (AC đã có), architecture.md, data-model.md, API.md | Kiro (Claude-based, chat) | 10 file story-specs/US-01…US-10.md: mỗi file có sequence diagram Mermaid, danh sách API calls, DB changes, error handling, edge cases | Tuyết chọn ngẫu nhiên US-04 (Policy Check) và US-07 (Expense Claim) — trace từ AC Given/When/Then → API sequence → Prisma query để đảm bảo không có step logic bị bỏ sót | Chấp nhận sau khi yêu cầu AI bổ sung bước `SELECT FOR UPDATE` trong transaction của US-04 để tránh race condition khi 2 Manager submit cùng lúc; đây là gap mà AI bỏ qua ở draft đầu | Viết tay ước tính 5 giờ → có AI hỗ trợ 2.5 giờ | `docs/05-technical/story-specs/US-01…US-10.md` |
| T-05 | 01/09/2026 | Scaffold Backend — package.json & tsconfig | Khởi tạo Node.js + Express + TypeScript project đúng stack đã chốt (architecture.md §2) | Tech stack: Node 20, Express 4, TypeScript 5, Prisma 5.22, tsx, zod, JWT, bcryptjs; môi trường: Windows, npm (pnpm không được cài) | Kiro (Agentic, autopilot) | `package.json` (22 dependencies đúng spec), `tsconfig.json` (target ES2020, CommonJS, strict mode, rootDir/outDir) | Tuyết kiểm tra: (1) `@prisma/client` ở `dependencies` (không phải `devDependencies`); (2) `strict: true` + `noImplicitAny` bật; (3) `tsc --noEmit` exit 0 | Chấp nhận; bỏ puppeteer khỏi dependencies scaffold ban đầu (cần download Chromium ~300MB, không phù hợp giai đoạn scaffold) — ghi nhận sẽ thêm lại khi implement PDF feature | Tự làm ước tính 45 phút → có AI hỗ trợ 10 phút | `src/backend/package.json`, `src/backend/tsconfig.json` |
| T-06 | 01/09/2026 | Express Server Entry Point (server.ts + app.ts) | Tạo Express app factory với middleware chain đúng thứ tự (architecture.md §5.2) và graceful shutdown | architecture.md §5.2 (middleware chain: requestLogger → cors → helmet → json → authGuard → roleGuard → immutableGuard), API.md §1 (base URL, error format) | Kiro (Agentic, autopilot) | `src/server.ts` (bootstrap + env validation + graceful shutdown + uncaughtException), `src/app.ts` (Express factory với 8 route groups), `src/middlewares/` (logger.ts, error-handler.ts, not-found-handler.ts) | Tuyết chạy `tsc --noEmit` → exit 0; khởi động server → `GET /health` trả về `200 OK`; gọi route không tồn tại → `404 ROUTE_NOT_FOUND` có `requestId`; kiểm tra Helmet headers có trong response | Chấp nhận; fix 1 lỗi nhỏ: AI import `NextFunction` thừa trong `app.ts` (không dùng trực tiếp) → `tsc --noEmit` báo lỗi TS6133 → sửa bỏ import | Tự làm ước tính 1.5 giờ → có AI hỗ trợ 30 phút | `src/backend/src/server.ts`, `src/backend/src/app.ts`, `src/backend/src/middlewares/` |
| T-07 | 01/09/2026 | Prisma Schema (SQLite — dev scaffold) | Tạo Prisma schema 10 models đúng data-model.md, chạy migrate tạo DB dev | data-model.md §3-§6 (Prisma Schema section), quyết định dùng SQLite thay PostgreSQL cho scaffold (không có PostgreSQL local, không có Docker) | Kiro (Agentic, autopilot) | `src/prisma/schema.prisma` (10 models: User, RefreshToken, Trip, PolicyCheckResult, ItineraryItem, ApprovalRecord, Expense, ExpenseItem, Notification, AuditLog), migration SQL, Prisma Client generated | Tuyết chạy `prisma validate` → valid; `prisma migrate dev --name init_schema` → exit 0, tạo migration.sql; kiểm tra migration.sql có đủ 10 `CREATE TABLE` statement đúng tên bảng | **Quyết định kỹ thuật:** SQLite không hỗ trợ native enum → dùng `String` cho tất cả enum fields, validation ở Service layer; `BigInt` VNĐ → `Int` cho SQLite compatibility (đủ cho demo ~2.1 tỷ VNĐ); ghi nhận sẽ migrate sang PostgreSQL (có `@db.Timestamptz`, `@db.Uuid`, enum native) khi deploy | Tự làm ước tính 2 giờ → có AI hỗ trợ 45 phút | `src/backend/src/prisma/schema.prisma`, `src/backend/src/prisma/migrations/` |
| T-08 | 01/09/2026 | Skeleton Files — Controllers, Services, Middlewares, Lib, Utils | Tạo cấu trúc thư mục đầy đủ theo architecture.md §2.2, implement business logic thật ở services | architecture.md §2.2 (folder structure), §5.3 (service descriptions), business-rules.md (BR-TR-01…07) | Kiro (Agentic, autopilot) | 35 TypeScript files: `controllers/` (8), `services/` (6), `middlewares/` (6), `lib/` (3), `utils/` (3), `prisma/client.ts` | Tuyết kiểm tra: (1) `policy.service.ts` implement đúng 4 BR (BR-TR-01 hotel limit, BR-TR-02 per diem cap, BR-TR-03 advance notice, BR-TR-04 level2 routing); (2) `approval.service.ts` logic `budget > 20M OR hasViolations → PENDING_ADMIN_APPROVAL`; (3) `expense.service.ts` variance thresholds 0%/10%; (4) `tsc --noEmit` exit 0 | Chấp nhận; fix 2 lỗi TypeScript strict: (1) unused `input` parameter trong `ai.client.ts` → đổi thành `_input`; (2) unused `Errors` import trong `immutable.guard.ts` (chưa implement) → comment out | Tự làm ước tính 3 giờ → có AI hỗ trợ 1 giờ | `src/backend/src/controllers/`, `src/backend/src/services/`, `src/backend/src/middlewares/`, `src/backend/src/lib/`, `src/backend/src/utils/` |
| T-09 | 01/09/2026 | Seed Data (6 users + 3 trips + related data) | Tạo dữ liệu mẫu đại diện đủ 5 roles, đủ trạng thái trip để test luồng nghiệp vụ | persona-jtbd.md (Nguyễn Văn Nam, Trần Đình Hùng, Lê Thị Mai, Phạm Thu Trang), business-rules.md (trần hotel, per diem, violation), Prisma schema 10 models | Kiro (Agentic, autopilot) | `src/prisma/seed.ts`: 6 users (đủ 5 roles), 3 trips (DRAFT/SUBMITTED/APPROVED), 1 PolicyCheckResult có violation BR-TR-01, 1 ApprovalRecord (LEVEL_1 APPROVED), 3 ItineraryItems, 1 Expense + 2 ExpenseItems, 3 Notifications | Tuyết chạy `npx tsx src/prisma/seed.ts` → output đúng "✓ 6 users / ✓ 3 trips..."; chạy lần 2 để verify idempotent (deleteMany trước seed) → không lỗi; kiểm tra trip SUBMITTED có hotel 1.200.000 VNĐ (vượt trần STAFF 1.000.000) đúng với PolicyCheckResult violation BR-TR-01 | Chấp nhận; fix lỗi `skipDuplicates` không được SQLite Prisma provider support → thay bằng `deleteMany` trước rồi `createMany` (seed idempotent theo cách khác) | Tự làm ước tính 1.5 giờ → có AI hỗ trợ 30 phút | `src/backend/src/prisma/seed.ts` |
| T-10 | 01/09/2026 | Env Config & Gitignore | Hoàn thiện .env, .env.example, .gitignore đảm bảo không leak secrets lên Git | Architecture.md §6 (JWT, bcrypt), API.md §1 (base URL, CORS), danh sách biến môi trường cần thiết từ server.ts | Kiro (Agentic, autopilot) | `src/backend/.env` (đầy đủ 8 biến), `src/backend/.env.example` (template có hướng dẫn generate JWT secret), `src/backend/.gitignore`, root `.gitignore`, root `.env.example` | Tuyết kiểm tra: (1) `.env` không bị Git track (`git status` không show file); (2) `.env.example` không chứa secret thật; (3) `*.db` file trong gitignore; (4) `prisma validate` vẫn pass sau khi cập nhật `.env` | Chấp nhận; bổ sung thêm `RATE_LIMIT_MAX` và `AUTH_RATE_LIMIT_MAX` vào `.env` sau khi kiểm tra `app.ts` có dùng giá trị này (AI bỏ sót biến này ở draft đầu) | Tự làm ước tính 30 phút → có AI hỗ trợ 10 phút | `src/backend/.env.example`, `src/backend/.gitignore`, `.gitignore`, `.env.example` |
| T-11 | 03/09/2026 | TSK-Auth: POST /auth/login + authGuard JWT thật | Implement toàn bộ luồng xác thực — login cấp JWT, refresh token, logout, authGuard verify thật — unblock tất cả protected routes | API.md §4 (Auth endpoints, response schema, httpOnly cookie spec), architecture.md §6 (JWT flow, HS256, 15m access + 7d refresh, timing-safe bcrypt), data-model.md §3.2 (refresh_tokens: hash SHA-256, is_revoked, expires_at) | Kiro (Agentic, autopilot) | `auth.service.ts` (login bcrypt timing-safe, JWT HS256 15m, refresh token SHA-256, audit log), `auth.guard.ts` (jwt.verify thật, map TokenExpiredError/JsonWebTokenError), `auth.controller.ts` (httpOnly cookie SameSite=Strict, rawRefreshToken không trong body, 204 logout), `auth.routes.ts` (public: login/refresh, protected: logout/me) | Tuyết: (1) chạy 17 test cases bằng curl — login 200 + token + cookie, sai pass→401, thiếu field→400, GET /me đúng role, POST /refresh→token mới, DELETE /logout→204; (2) kiểm tra passwordHash không trong response body; (3) kiểm tra rawRefreshToken không trong body; (4) tsc --noEmit exit 0 | Chấp nhận toàn bộ; phát hiện vấn đề DB bị mất (dev.db xóa khỏi folder) → chạy lại `prisma migrate dev + seed` để khôi phục trước khi test; fix PowerShell escape issue khi gọi curl với JSON body (dùng file tạm thay vì string literal) | Tự làm ước tính 3 giờ → có AI hỗ trợ 1 giờ | `src/backend/src/services/auth.service.ts`, `src/backend/src/middlewares/auth.guard.ts`, `src/backend/src/controllers/auth.controller.ts`, `src/backend/src/routes/auth.routes.ts` |
| T-12 | 03/09/2026 | TSK-102: POST /api/trips & DB Model Trips | Implement API tạo Trip Request (US-01): Zod validation, isUrgent server-computed, per diem soft warning, audit log, RBAC EMPLOYEE-only | US-01-create-trip-request.md (acceptance criteria đầy đủ, validation matrix TC-D01..D11 + TC-P01..P10), API.md §5 (POST /trips request/response schema, server-computed fields list), business-rules.md (BR-TR-02 per diem, BR-TR-03 advance notice) | Kiro (Agentic, autopilot) | `trip.validator.ts` (Zod schema: origin/destination 1-200, destinationType enum, departureDate >= today, returnDate >= departureDate, purpose 10-1000, estimatedBudget int > 0, optional int >= 0; checkPerDiemWarning soft validation BR-TR-02), `trip.service.ts` createTrip() (isUrgent từ countWorkingDays, urgencyReason required, INSERT DB, logAudit), `trip.controller.ts` createTrip() (thin wrapper, 201 + warnings), `trips.routes.ts` (POST / với authGuard + roleGuard(['EMPLOYEE'])) | Tuyết chạy 16 test cases: T1.1 (201 DRAFT tripDays=3 UUID), T1.2 (per diem đúng hạn, no warning), T1.3 (per diem vượt → 201 + POLICY_VIOLATION_PER_DIEM_EXCEEDED), T1.4 (urgent thiếu reason → 400), T1.5 (urgent có reason → 201 isUrgent=true), T1.6-T1.8 (validation 400s), T1.9 (Manager → 403), T1.10 (no token → 401), T1.11 (audit log TRIP_CREATED trong DB), T1.13 (server strip tripDays=999 → 3) | Chấp nhận; phát hiện và fix lỗi VALID_TRANSITIONS khai báo 2 lần trong trip.service.ts do str_replace không đúng vị trí — dùng fs_write viết lại toàn bộ file; fix compile lỗi TS2451 bằng cách xóa dist/ cache cũ và mở terminal mới | Tự làm ước tính 4 giờ → có AI hỗ trợ 1.5 giờ | `src/backend/src/utils/validators/trip.validator.ts`, `src/backend/src/services/trip.service.ts`, `src/backend/src/controllers/trip.controller.ts`, `src/backend/src/routes/trips.routes.ts` |
| T-13 | 03/09/2026 | Phát triển toàn bộ routes còn lại (Bước 1-16) | Implement đầy đủ 35 endpoints theo API.md: trips CRUD, submit/approve/reject/close, itinerary, expenses, notifications+SSE, dashboard, PDF export | API.md §5-§13 (tất cả endpoint specs, role permission matrix), business-rules.md (BR-TR-04 approval routing, BR-TR-05 variance, BR-TR-06 immutable), data-model.md (Prisma queries), user-stories.md (US-03..US-10) | Kiro (Agentic, autopilot) | 16 service/controller/route files mới: `itinerary.service.ts`, `expense.service.full.ts`, `notification.controller.ts`, `dashboard.controller.ts`, `pdf.controller.ts` + routes tương ứng; trip.service.ts hoàn chỉnh (getAllTrips RBAC, submitTrip PolicyCheck, approveTrip ApprovalRouter, rejectTrip, closeTrip); immutableGuard đầy đủ (Prisma query BR-TR-06); SSE emit kích hoạt trong notification.service.ts | Tuyết chạy smoke test 24 cases: S01-S24 bao gồm auth endpoints, trips CRUD, RBAC checks (MGR→403, FIN→403), itinerary, expenses owner check, notifications, dashboard tất cả roles, PDF export owner+Finance, health check, 404 unknown route — **24/24 PASS** | Chấp nhận toàn bộ; fix 2 lỗi: (1) `sendPaginated` import thừa trong notification.controller → TS6133; (2) `$EMP2` token dùng trước khi khai báo trong smoke_test.ps1 → S13 fail; vấn đề `curl` vs `curl.exe` trong PowerShell (alias conflict) phát hiện khi debug server connection | Tự làm ước tính 12 giờ → có AI hỗ trợ 4 giờ | `src/backend/src/services/`, `src/backend/src/controllers/`, `src/backend/src/routes/`, `src/backend/src/middlewares/immutable.guard.ts` |

---

## Ghi chú

- File này ghi riêng AI usage của **Tuyết (Engineering)**, bổ sung cho log nhóm `AI_USAGE_LOG.md`.
- Mỗi entry trong file này cũng phải được cross-reference vào `AI_USAGE_LOG.md` ở các entry liên quan (A-10, A-12, T-01…T-12).
- **T-03** là ví dụ **từ chối một phần đề xuất AI** — AI gộp approve L1/L2 không đủ RBAC ở tầng middleware, Tuyết phát hiện và yêu cầu sửa lại để enforce role check đúng tầng.
- **T-07** là ví dụ **quyết định kỹ thuật có cân nhắc**: SQLite thay PostgreSQL cho dev scaffold — AI đề xuất theo spec (PostgreSQL), Tuyết quyết định dùng SQLite vì môi trường thực tế không có PostgreSQL/Docker, có ghi nhận trade-off và kế hoạch migrate.
- **T-12** là ví dụ **AI gây lỗi kỹ thuật cần debug**: `VALID_TRANSITIONS` bị khai báo duplicate sau str_replace sai vị trí → tsc báo TS2451 → Tuyết phát hiện qua đọc file, dùng fs_write viết lại toàn bộ để fix, xóa dist/ cache để compiler đọc source mới.
- **T-13** là ví dụ **scale lớn nhất**: implement 35 endpoints trong 1 session — AI generate toàn bộ service/controller/routes, Tuyết verify bằng smoke test 24 cases tự động, sửa 2 lỗi nhỏ (import thừa + token scope), kết quả 24/24 PASS.
- File phải cập nhật **liên tục** mỗi khi Tuyết dùng AI để tạo/sửa artifact kỹ thuật: code backend, test, CI/CD config, security fix, performance optimization.
